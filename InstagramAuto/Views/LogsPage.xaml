<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="InstagramAuto.Client.Views.LogsPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="لاگ‌ها">

    <StackLayout Padding="20" Spacing="10">
        <Label Text="لاگ‌ها" FontSize="22" HorizontalOptions="Center" />

        <!-- Error message and copy button -->
        <StackLayout
            IsVisible="{Binding HasError}"
            BackgroundColor="#fff0f0"
            Padding="8"
            Spacing="4">
            <Label
                Text="{Binding ErrorMessage}"
                TextColor="Red"
                FontAttributes="Bold" />

            <Button
                Text="کپی خطا"
                Clicked="OnCopyErrorClicked"
                FontSize="12"
                BackgroundColor="#eee"
                TextColor="Black"
                Padding="6,2"
                HorizontalOptions="End" />

            <Editor
                Text="{Binding ErrorDetails}"
                IsReadOnly="True"
                AutoSize="TextChanges"
                HeightRequest="120"
                FontSize="12"
                BackgroundColor="#f8f8f8"
                TextColor="Black" />
        </StackLayout>

        <!-- Logs list -->
        <CollectionView
            ItemsSource="{Binding Items}"
            RemainingItemsThreshold="10"
            RemainingItemsThresholdReachedCommand="{Binding LoadMoreCommand}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame
                        Margin="0,4"
                        Padding="8"
                        BorderColor="#ddd"
                        CornerRadius="8">
                        <StackLayout>
                            <Label
                                Text="{Binding Timestamp}"
                                FontSize="12"
                                TextColor="#888" />
                            <Label
                                Text="{Binding Level}"
                                FontSize="14"
                                FontAttributes="Bold" />
                            <Label
                                Text="{Binding Message}"
                                FontSize="14" />
                        </StackLayout>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </StackLayout>
</ContentPage>
