<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="InstagramAuto.Client.Views.SessionManagerPage"
             Title="?????? ?????? / Session Manager">
    <ScrollView>
        <StackLayout Padding="16" Spacing="14">
            <Label Text="?????? ??????? ????" FontSize="22" HorizontalOptions="Center" />
            <CollectionView ItemsSource="{Binding Sessions}" HeightRequest="300">
                <CollectionView.Header>
                    <Grid Padding="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Label Text="?????" Grid.Column="0" FontAttributes="Bold" />
                        <Label Text="?????" Grid.Column="1" FontAttributes="Bold" />
                        <Label Text="??????" Grid.Column="2" FontAttributes="Bold" />
                    </Grid>
                </CollectionView.Header>
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Label Text="{Binding AccountId}" Grid.Column="0" />
                            <Label Text="{Binding CreatedAt, StringFormat='{0:yyyy/MM/dd HH:mm}'}" Grid.Column="1" />
                            <Button Text="???" Command="{Binding BindingContext.RemoveSessionCommand, Source={x:Reference Name=SessionManagerPage}}" CommandParameter="{Binding .}" Grid.Column="2" />
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </StackLayout>
    </ScrollView>
</ContentPage>
