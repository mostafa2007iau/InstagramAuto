<?xml version="1.0" encoding="utf-8" ?>
<!--
Persian:
    ???? ???? ???? ? ????? ????? ?????? (CSV/JSON).
English:
    Page for importing and exporting rules (CSV/JSON).
-->
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="InstagramAuto.Client.Views.ImportExportPage"
             Title="Import/Export Rules">
    <ContentPage.BindingContext>
        <!-- ViewModel will be set in code-behind via DI -->
    </ContentPage.BindingContext>
    <ScrollView>
        <StackLayout Padding="16" Spacing="12">
            <Label Text="???? ????/????? ????? ?????? / Import/Export Rules" FontSize="22" HorizontalOptions="Center" />
            <Button Text="????? ????? ?? JSON" Command="{Binding ExportJsonCommand}" />
            <Button Text="????? ????? ?? CSV" Command="{Binding ExportCsvCommand}" />
            <Button Text="???? ???? ?? JSON" Command="{Binding ImportJsonCommand}" />
            <Button Text="???? ???? ?? CSV" Command="{Binding ImportCsvCommand}" />
            <Editor HeightRequest="120" Text="{Binding DataText}" Placeholder="???? ?????? (???? ???? ???? ?? ?????? ?????)" />
            <Label Text="{Binding ErrorMessage}" TextColor="Red" IsVisible="{Binding HasError}" />
        </StackLayout>
    </ScrollView>
</ContentPage>
