"""
Persian:
    ????? ????? ?????? ?? ????? ??????????.
English:
    Service for sending DM to comment user.

Install:
    pip install instagrapi
"""

class DMService:
    """
    Persian:
        ????? ????? ?????? ?? ????? ??????????.
    English:
        Service for sending DM to comment user.
    """
    async def send_dm(self, client, user_id, rule):
        # Send DM as per rule (text, link, image, etc.)
        # TODO: Implement random DM selection and media/link support
        await client.direct_send(rule.get_random_dm(), [user_id])
